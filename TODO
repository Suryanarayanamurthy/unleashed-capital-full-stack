prototype api-gateway mvp:
  
prototype auth with passport
prototype auth to use db
~~~~~~~~  
prototype FE

install, build and  run/deploy
  - shell script to start servers locally
  - docker-compose for starting multi container
    - dockerize stuff
  - vagrantfile with docker swarm mode to 
---------------------


cleanup crap:
 - clean up readme
 - es6 the code and rm clutter.
